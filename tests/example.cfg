# Example config file that uses the example jobs (example_station_A,
# example_station_B and example_ana_A) and the tests/fakelims.py
# server.

# put everything in one big stanza
[DEFAULT]
unit_type = CCD
unit_id = 1-100
lims_url = http://localhost:9876/
stage_root = /tmp/example_lcatr/stage
archive_root = /tmp/example_lcatr/archive
archive_host = localhost
archive_user = bviren
